<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <style>
        body { font-family: 'Segoe UI', Arial, sans-serif; background: #0f172a; color: #e2e8f0; margin: 0; padding: 0; }
        .container { max-width: 560px; margin: 40px auto; background: #1e293b; border-radius: 16px; overflow: hidden; border: 1px solid rgba(245,158,11,0.2); }
        .header { background: linear-gradient(135deg, #f59e0b, #d97706); padding: 32px; text-align: center; }
        .header h1 { margin: 0; font-size: 22px; color: #000; }
        .header p { margin: 8px 0 0; color: rgba(0,0,0,0.7); font-size: 14px; }
        .body { padding: 32px; }
        .badge { display: inline-block; background: linear-gradient(135deg, #6366f1, #8b5cf6); color: #fff; padding: 6px 16px; border-radius: 999px; font-weight: 700; font-size: 13px; margin-bottom: 20px; }
        .info-box { background: rgba(245,158,11,0.08); border: 1px solid rgba(245,158,11,0.2); border-radius: 12px; padding: 20px; margin: 16px 0; }
        .info-box .label { font-size: 12px; text-transform: uppercase; color: #94a3b8; letter-spacing: 1px; margin-bottom: 4px; }
        .info-box .value { font-size: 18px; font-weight: 700; color: #f1f5f9; }
        .cta { display: inline-block; background: linear-gradient(135deg, #22c55e, #16a34a); color: #fff; padding: 12px 28px; border-radius: 12px; font-weight: 700; text-decoration: none; margin-top: 20px; }
        .footer { text-align: center; padding: 20px 32px; font-size: 12px; color: #64748b; border-top: 1px solid rgba(255,255,255,0.05); }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <img src="cid:logo" alt="E-Sports Platform" style="width:80px;height:80px;margin-bottom:12px;">
            <h1>📩 Nouvelle demande d'adhésion</h1>
            <p>Un joueur souhaite rejoindre votre équipe</p>
        </div>
        <div class="body">
            <span class="badge">⏳ En attente</span>

            <div class="info-box">
                <div class="label">Joueur</div>
                <div class="value">{{ requesterName }}</div>
            </div>

            <div class="info-box">
                <div class="label">Équipe</div>
                <div class="value">{{ teamName }}</div>
            </div>

            <p style="color:#94a3b8;line-height:1.7;">
                Rendez-vous sur le dashboard de votre équipe pour accepter ou refuser cette demande.
            </p>
        </div>
        <div class="footer">
            E-Sport Platform — Ne pas répondre à cet email
        </div>
    </div>
</body>
</html>
